# Webhooks - Ejemplo en Node.js

Este proyecto implementa un servidor de webhook básico usando Node.js y Express. El servidor recibe eventos en un endpoint `/webhook` y responde con un mensaje.

## **Requisitos**

- Node.js instalado (v14+)
- npm instalado

## **Instrucciones**

1. Instala las dependencias:
   ```bash
   npm install express

